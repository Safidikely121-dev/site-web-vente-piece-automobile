# Variable d'environnement Frontend (optionnelle)
# Le frontend utilise le proxy Vite pour /api (voir vite.config.ts).
# En production derrière un reverse-proxy, VITE_API_URL peut servir de base URL si le code l'utilise.
VITE_API_URL=/api